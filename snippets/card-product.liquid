<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Your Shopify Page</title>
  <style>
    /* Your existing styles */
    .animate-expand {
      overflow: hidden;
      transition: opacity 3s;
      opacity: 0;
    }

    .animate-expand:hover {
      opacity: 1;
    }

    .transform-translate {
      transform: translateX(-100%);
      opacity: 0;
      transition: transform 0.5s ease-out, opacity 0.5s ease-out;
    }

    .transform-translate:not(.in-view) {
      transform: translateX(5%);
      opacity: 1;
    }

    /* Converted Tailwind CSS */
    #product-card {
      width: 100%;
      padding: 6px 3px;
      overflow: hidden;
    }

    #product-card .border {
      border-width: 4px;
      border-style: solid;
      border-color: #F3EFEF; /* Assuming #F3EFEF is the color for border-yellow-200 */
    }

    #product-card .shadow-lg {
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

    #product-card .rounded-lg {
      border-radius: 0.375rem; /* Assuming 0.375rem is the border-radius for rounded-lg in Tailwind */
    }

    #product-card .h-80 {
      height: 20rem; /* Assuming 20rem is the height for h-80 in Tailwind */
    }

    #product-card .object-cover {
      object-fit: cover;
    }

    #product-card .transition {
      transition-property: all;
      transition-duration: 1000ms;
      transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
    }

    #product-card .ease-in-out {
      transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
    }

    #product-card .hover\:scale-105:hover {
      transform: scale(1.05);
    }

    #product-card .flex {
      display: flex;
    }

    #product-card .flex-col {
      flex-direction: column;
    }

    #product-card .p-4 {
      padding: 1rem;
    }

    #product-card .text-sm {
      font-size: 0.875rem; /* Assuming 0.875rem is the font-size for text-sm in Tailwind */
    }

    #product-card .text-gray-400 {
      color: #717171; /* Assuming #717171 is the color for text-gray-400 in Tailwind */
    }

    #product-card .uppercase {
      text-transform: uppercase;
    }

    #product-card .font-medium {
      font-weight: 500;
    }

    #product-card .text-gray-700 {
      color: #2D2D2D; /* Assuming #2D2D2D is the color for text-gray-700 in Tailwind */
    }

    #product-card .text-gray-500 {
      color: #545454; /* Assuming #545454 is the color for text-gray-500 in Tailwind */
    }
  </style>
</head>
<body>
  <!-- Your Shopify HTML content -->
  <div id="product-card">
    <!-- Replace the content below with your actual Shopify code -->
    <div class="border bg-white shadow-lg rounded-lg overflow-hidden">
      <a href="{{ product_product.url }}">
        {% if product_product.featured_image %}
          <div class="h-80 overflow-hidden">
            <img class="w-full h-full object-cover transition duration-1000 ease-in-out hover:scale-105 transform" src="{{ product_product.featured_image | img_url: 'medium' }}" alt="{{ product_product.featured_image.alt }}">
          </div>
        {% else %}
          {{ 'product-1' | placeholder_svg_tag: 'w-full h-full object-cover bg-gray-100' }}
        {% endif %}
      </a>
      
      <div class="flex flex-col p-4">
        <span class="text-sm text-gray-400">{{ product_product.vendor }}</span>
        <a href="{{ product_product.url }}" class="text-gray-700 uppercase font-medium">{{ product_product.title }}</a>
        <p class="text-gray-500">{{ product_product.price | money }}</p>
      </div>
    </div>
  </div>
</body>
</html>
